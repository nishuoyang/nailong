version: '3.8'

services:
  server:
    build:
      context: .
      dockerfile: server/Dockerfile
    container_name: nailong-server
    restart: unless-stopped
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - sqlite_data:/app/data
      - ./server/.env:/app/.env:ro
      # SSL 证书（卷挂载，不进镜像层）
      - ./static/nailonghub.top.key:/app/static/nailonghub.top.key:ro
      - ./static/nailonghub.top.pem:/app/static/nailonghub.top.pem:ro
      # 客户端构建产物（需先 cd client && npm run build）
      - ./client/dist:/app/client-dist:ro
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/data/data.db
      # HTTPS
      - PORT=443
      - HTTPS_ENABLED=true
      - SSL_KEY_PATH=/app/static/nailonghub.top.key
      - SSL_CERT_PATH=/app/static/nailonghub.top.pem
      - REDIRECT_HTTP_PORT=80
      # 静态文件
      - CLIENT_DIST_PATH=/app/client-dist
      # MinIO（容器内互联）
      - MINIO_ENDPOINT=minio
      - MINIO_PORT=9000
      - MINIO_ACCESS_KEY=minioadmin
      - MINIO_SECRET_KEY=minioadmin
      - MINIO_BUCKET=nailong-images
      # MinIO 公网 URL（同源代理，避免 mixed content）
      - MINIO_PUBLIC_URL=https://nailonghub.top/minio

  redis:
    image: redis:7-alpine
    container_name: nailong-redis
    restart: unless-stopped
    volumes:
      - redis_data:/data

  minio:
    image: minio/minio:latest
    container_name: nailong-minio
    command: server /data --console-address ":9001"
    restart: unless-stopped
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio_data:/data

volumes:
  sqlite_data:
  redis_data:
  minio_data:
